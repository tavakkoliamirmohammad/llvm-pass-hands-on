int compute(int a, int b) {
    int result = (a / a);
    result *= 0;
    result *= 1;
    result /= 1;
    result *= (b / b);
    result += (b - b);
    result /= result;
    result -= result;
    result += 4;
    result *= 2;
    result /= 1;
    return result;
}